import 'package:flutter/material.dart';

class DreamLogScreen extends StatelessWidget {
  const DreamLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dream Log Screen'),
      ),
    );
  }
}
